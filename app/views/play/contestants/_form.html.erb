<%= form_with(model: contestant, url: url_for(controller: 'play/contestants', action: 'create', subdomain: 'play')) do |form| %>
  <% if contestant.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(contestant.errors.count, "error") %> prohibited this contestant from being saved:</h2>

      <ul>
        <% contestant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

    <%= form.hidden_field :tournament_id, value: @tournament.id %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
